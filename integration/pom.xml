<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>info.rmapproject</groupId>
        <artifactId>rmap-parent</artifactId>
        <version>1.0.1-beta-SNAPSHOT</version>
    </parent>

    <artifactId>rmap-integration</artifactId>
    <name>RMap Integration</name>
    <packaging>jar</packaging>

    <properties>
        <rmap.webapp.context>/app</rmap.webapp.context>
        <rmap.api.context>/api</rmap.api.context>
    </properties>

    <dependencies>

        <dependency>
            <groupId>info.rmapproject</groupId>
            <artifactId>rmap-webapp</artifactId>
            <version>${project.parent.version}</version>
            <type>war</type>
        </dependency>

        <dependency>
            <groupId>org.apache.derby</groupId>
            <artifactId>derby</artifactId>
        </dependency>

        <dependency>
            <groupId>info.rmapproject</groupId>
            <artifactId>rmap-api</artifactId>
            <version>${project.parent.version}</version>
            <type>war</type>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
        </dependency>

        <dependency>
            <groupId>info.rmapproject</groupId>
            <artifactId>rmap-core</artifactId>
            <version>${project.parent.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>
    
    <build>

        <plugins>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>reserve-port</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>reserve-network-port</goal>
                        </goals>
                        <configuration>
                            <portNames>
                                <portName>rmap.webapp.test.port</portName>
                            </portNames>
                        </configuration>
                    </execution>
                </executions>
            </plugin>


            <plugin>
                <groupId>org.codehaus.cargo</groupId>
                <artifactId>cargo-maven2-plugin</artifactId>
                <configuration>
                    <!-- Container configuration -->
                    <container>
                        <containerId>jetty9x</containerId>
                        <type>installed</type>
                        <systemProperties>
                            <!-- <cargo.jvmargs>-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=y,address=5005</cargo.jvmargs> -->
                            <cargo.servlet.port>${rmap.webapp.test.port}</cargo.servlet.port>
                            <derby.system.home>${project.build.directory}/derby</derby.system.home>
                            <logback.configurationFile>${project.build.outputDirectory}/logback.xml</logback.configurationFile>
                            <spring.profiles.active>default,inmemory-db,inmemory-idservice,inmemory-triplestore,persistent-userservice</spring.profiles.active>
                        </systemProperties>
                        <dependencies>
                            <dependency>
                                <groupId>org.apache.derby</groupId>
                                <artifactId>derby</artifactId>
                            </dependency>
                        </dependencies>
                    </container>
                    <deployables>

                        <deployable>
                            <groupId>info.rmapproject</groupId>
                            <artifactId>rmap-webapp</artifactId>
                            <type>war</type>
                            <properties>
                                <context>${rmap.webapp.context}</context>
                            </properties>
                        </deployable>

                        <deployable>
                            <groupId>info.rmapproject</groupId>
                            <artifactId>rmap-api</artifactId>
                            <type>war</type>
                            <properties>
                                <context>${rmap.api.context}</context>
                            </properties>
                        </deployable>


                    </deployables>

                    <configuration>
                        <properties>
                            <!-- <cargo.jvmargs>-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=y,address=5005</cargo.jvmargs> -->
                            <!--<cargo.runtime.args>&#45;&#45;ini &#45;&#45;module=client &#45;&#45;module=annotations &#45;&#45;module=jmx &#45;&#45;module=server &#45;&#45;module=deploy &#45;&#45;module=websocket &#45;&#45;module=jsp &#45;&#45;module=ext &#45;&#45;module=resources &#45;&#45;module=http &#45;&#45;module=plus</cargo.runtime.args>-->
                            <cargo.jvmargs>-Dorg.apache.tomcat.util.buf.UDecoder.ALLOW_ENCODED_SLASH=true -Djava.util.logging.config.file=${project.basedir}/src/test/resources/logging.properties</cargo.jvmargs>
                            <cargo.servlet.port>${rmap.webapp.test.port}</cargo.servlet.port>
                        </properties>
                    </configuration>

                </configuration>
                <executions>
                    <execution>
                        <id>start-container</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>start</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>stop-container</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>stop</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>default-cli</id>
                        <configuration>
                            <container>
                                <systemProperties>
                                    <spring.profiles.active>default,inmemory-db,inmemory-idservice,inmemory-triplestore,persistent-userservice</spring.profiles.active>
                                    <hibernate.hbm2ddl.auto>create</hibernate.hbm2ddl.auto>
                                    <rmap.configFile>file:///${project.basedir}/src/main/resources/rmap.properties</rmap.configFile>
                                    <logback.configurationFile>${project.basedir}/src/main/resources/logback.xml</logback.configurationFile>
                                </systemProperties>
                            </container>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <systemProperties>
                        <rmap.webapp.test.port>${rmap.webapp.test.port}</rmap.webapp.test.port>
                        <rmap.webapp.context>${rmap.webapp.context}</rmap.webapp.context>
                        <rmap.api.context>${rmap.api.context}</rmap.api.context>
                        <spring.profiles.active>default,inmemory-db,inmemory-idservice,inmemory-triplestore,persistent-userservice</spring.profiles.active>
                    </systemProperties>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
</project>
